spring.datasource.url=jdbc:mysql://localhost/springboot?useUnicode=true&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

#hibernate配置，自动创建、更新、验证数据库表结构(create、create-drop、update、validate)
#update 根据model类创建新表并自动更新已有表结构。不管是update或alidate多次执行同一save方法每次都会插入同样的值
spring.jpa.properties.hibernate.hbm2ddl.auto=update

